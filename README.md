# Overview: Swift for Android Exploration (Skip.tools)

## 1. Inventory of Current Sample Apps
This is an overview of the current proof-of-concepts (PoCs) developed to validate Skip's functionality.

* **[Rent a car App Skip Fuse]**
    * *Description:* This project uses Skip Fuse to translate the business logic to Kotlin. This means that the business logic is natively compiled to binary code and shared between both platforms. The UI is seperate for both iOS and Android. All the iOS and shared code is in the Sources folder. All the Android code is in the Android folder which you can open separately in Android studio. You can find all the generated Kotlin code in the skipstone folder in Android Studio.

## 2. Architecture: Shared vs. Platform-Specific Code
The project uses the Skip native compiler to maintain a shared codebase.

* **Shared Code (Swift):**
    * Business logic and data models (ViewModels).
* **iOS-Specific (Swift/Xcode):**
    * Platform-specific SwiftUI views.
    * iOS-specific permissions (Info.plist) and configurations.
* **Android-Specific (Kotlin/Android Studio):**
    * Native Jetpack Compose views.
    * Kotlin files generated by Skip.
    * Android Gradle build scripts (`build.gradle.kts`), `AndroidManifest.xml`, and Fastlane configurations (`Fastfile`, `Appfile`).

## 3. Build and Run Instructions
Instructions to build and run the app locally.

**For iOS:**
1.  Open the project in Xcode.
2.  Select the desired iOS simulator or physical device.
3.  Make sure an Android emulator is running, otherwise the build will fail.
4.  Press `Cmd + R` (Run) to build and start the application.

**For Android:**
1.  Make sure that you have built the source code in xcode at least once before you start.
2.  Open the `Android/` folder of the project in Android Studio.
3.  Allow Gradle to sync (this processes the Skip-generated Kotlin code).
4.  Select an Android Emulator or physical device and click 'Run' (`Shift + F10`).


## 4. Known Issues & Open Questions

**Known Issues:**
* There are a few issues with the Android UI. Not everything in Android works or is consistent but that is not important for now because that has nothing to do with Skip. I have no experience with Android development thats why the UI has more bugs than the iOS version. The main concern is that the business logic works on Android and that is the case. 

**Note:**
* Google maps is not implemented yet in the Android version. 

**Open Questions for Reviewers:**
* https://icthva-my.sharepoint.com/:w:/g/personal/milad_ahmad_hva_nl/IQDIcWrRQz5mQaaXud5vzZerAZPkIX9cEonXKThuU7LzUgs?e=uENibx
